
.button {
  border-radius 3px;
  color orange3;
  background rgba(orange3, 0);
  border 1px solid orange3;
  padding 8px 30px;
  font-weight 600;
  transition background 300ms, color 300ms;

  &:hover {
    background rgba(orange3, 1);
    color white;
  }
}